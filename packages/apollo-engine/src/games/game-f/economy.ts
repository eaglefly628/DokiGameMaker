// Game F · 经济/商店/升星数值（从 blueprint.ts 拆出）。纯数据叶子：被 combat（升星模板）/ blueprint（build 体）复用。

// ── 商店牌袋（F-11 / REQ-F-040 + v2 §4.6）：英雄码 + 单人有限牌袋（预洗、确定性；§4.4 牌袋语义）──
// 码 0 保留为「无」（bought_code 复位值）。6 将库牌袋（码 1..6 各 7 张；有限袋按 §4.6 语义随对局消耗，卖出归还袋底）。
// F-17 升星后 3 星需同将 9 张 → 袋按需扩；**只追加不重排**——前 24 张次序锁死既有验收断言。
export const SHOP_DECK = [3, 1, 5, 2, 6, 4, 1, 4, 2, 6, 3, 5, 5, 2, 1, 6, 4, 3, 2, 5, 3, 1, 6, 4, 4, 3, 6, 2, 1, 5, 6, 1, 4, 3, 5, 2, 3, 6, 2, 5, 1, 4];

// ── 升星数值（F-17/REQ-F-046，§4.6）：每星 血 ×1.8 / 攻与大招 ×1.5；卖价 1星=现行2金、2星=3×3−1、3星=3×9−1。──
export const STAR_HP_MUL = [0, 1, 1.8, 3.24]; // 索引=星级
export const STAR_DMG_MUL = [0, 1, 1.5, 2.25];
export const SELL_PRICE = [0, 2, 8, 26];
export const STAR_GLYPH = ['', '', '★★', '★★★'];
export const STAR_SCALE = [1, 1, 1.18, 1.38]; // marker 按星级放大（索引=星级）——升星一眼可辨（用户报「看不出升级」）
